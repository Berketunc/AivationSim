aruco_detector_node:
  ros__parameters:
    # Confirmed from arucotag/arucotag.png + model.sdf inspection:
    #   DICT_4X4_50, ID 0, plane size 0.5 m × 0.5 m (fills the entire ground plane)
    marker_id: 0
    aruco_dict: 'DICT_4X4_50'

    # Physical side length of the marker as rendered in Gazebo (= plane <size> 0.5 0.5)
    marker_size_m: 0.5
